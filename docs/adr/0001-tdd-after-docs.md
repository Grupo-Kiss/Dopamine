# TDD after documentation is complete

Implementation begins only after defs/ specs are complete enough for the current slice. Delivery uses strict test-driven development (failing test → minimal code → refactor). Chosen so behavior is locked by tests against the written specs rather than ad-hoc coding.
