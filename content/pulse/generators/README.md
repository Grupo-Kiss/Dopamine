Generator seeds for algorithmic Pulse posts and template variable fills.

`seeds.json` provides:

- `nouns`, `places`, `topics`, `takes`, `emoji` — fill `{noun}`, `{place}`, `{topic}`, `{take}`, `{emoji}` in `posts.json`
- `{user}` resolves from `users.json` handles

See `defs/08_content.md`.
