# Pulse — text library + generators

All text. No audio/video required.

- `posts.json` — authored posts (`rewardable: false` for ads/spam)
- `trends.json` — trend topics
- `users.json` — fictional handles
- `generators/seeds.json` — fragments for runtime generation

See `defs/08_content.md`.
