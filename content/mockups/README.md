# Mockups

Screen & chrome references for offline review.

- Mid-fi starters may still be present until the Opus pass replaces them.
- **Production checklist + required outputs:** `defs/14_visual_assets.md`
- Feel / palette language: `defs/09_game_feel.md`
- Approved Start Page composition refs: `.impeccable/mocks/`

Prefer shipping both SVG (structure) and WebP (hi-fi look) where `14` asks for both.

## Hi-fi status

Hi-fi surfaces are authored at **1920×1080** so they double as full-viewport comps. The SVG is the editable master; the WebP is a render of it, so re-render the WebP after editing an SVG.

| File | Phase | Status |
| --- | --- | --- |
| `start_page.svg` / `.webp` | A | hi-fi |
| `credits_modal.svg` / `.webp` | A | hi-fi |
| `splash.svg` / `.webp` | A | hi-fi |
| `game_over.svg` / `.webp` | A | hi-fi |
| `desktop_layout.svg` | B | mid-fi |
| `mobile_layout.svg` | B | not created |
| `dopamine_bar.svg` | B | mid-fi |
| `stickers_combo.svg`, `stickers_chain.svg` | B | mid-fi |
| `overlay_burnout.svg`, `overlay_recovery.svg` | B | mid-fi |
| `window_loop|pulse|wave|echo|alerts.svg` | C | not created |
| `minigame_lane.svg`, `minigame_blocks.svg` | E | mid-fi |
| `minigame_runner.svg` | E | not created |
