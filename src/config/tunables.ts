/**
 * Owner-editable product knobs — keep titles, copy, and balance numbers here
 * so tuning does not require hunting through components.
 *
 * Balance constants from defs/06 land here (or beside this file) as they are
 * implemented. Prefer the same names as the defs (DOPAMINE_MAX, …).
 */

/** Display name for the game. Change this when renaming. */
export const GAME_TITLE = 'Dopamine'

/** localStorage key for Accessibility Mode (reduced motion). */
export const ACCESSIBILITY_MODE_STORAGE_KEY = 'dopamine.accessibilityMode'

/** Quiet whisper under the title — keep empty to stay CTA-first. */
export const START_PAGE_WHISPER = ''
